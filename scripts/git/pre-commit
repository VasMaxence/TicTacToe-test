#!/bin/bash

# Translation Validation Hook
# Ensures all translation files are synchronized before commit

echo "🔍 Validating translations..."

dart scripts/validate_translations.dart

if [ $? -ne 0 ]; then
  echo "❌ Error: Translation validation failed. Please fix the translation files before committing."
  exit 1
fi

echo "✅ Translation validation passed!"
exit 0
